/**
 * AI TEAM READ — no live injury/form feed yet.
 * Hidden until a real payload exists so “coming soon” never competes with the stamp.
 */
export default function AiTeamReadPanel() {
  return null;
}
